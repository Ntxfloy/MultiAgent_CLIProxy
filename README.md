# 🤖 MultiAgent System + CLIProxy + Cyberpunk Chat

Мультиагентная система на базе AutoGen 0.4.x с интеграцией CLIProxy и веб-интерфейсом в стиле киберпанк.

## 🌟 Особенности

### Мультиагентная система
- **Асимметричный интеллект**: дешевые кодеры (gemini-2.5-flash) + дорогой ревьюер (gpt-5.2-codex)
- **SelectorGroupChat**: динамический выбор агентов на основе контекста
- **Resilient Client**: автоматические fallback цепочки при сбоях моделей
- **15 рабочих моделей**: GPT-5.x, Gemini 2.5, и другие через CLIProxy

### Веб-интерфейс
- **Cyberpunk UI**: неоновые цвета, кастомные scrollbars, анимации
- **Markdown рендеринг**: заголовки, списки, code blocks с подсветкой синтаксиса
- **Fullscreen layout**: адаптивный дизайн для desktop и mobile
- **История разговоров**: сохранение в localStorage
- **Выбор моделей**: переключение между 15 моделями в реальном времени
- **Многострочный ввод**: Shift+Enter для переноса строки

## 📦 Структура проекта

```
newMulti_v2/
├── agents/           # Определения агентов
│   └── registry_v3.py
├── core/             # Ядро системы
│   ├── engine.py
│   ├── resilient_client.py
│   └── swarm.py
├── tools/            # Инструменты для агентов
│   └── file_ops.py
├── workspace/        # Веб-приложение
│   ├── backend/      # FastAPI сервер
│   │   ├── app/
│   │   │   ├── main.py
│   │   │   ├── routes/
│   │   │   ├── services/
│   │   │   └── models.py
│   │   └── requirements.txt
│   ├── frontend/     # React + Vite
│   │   ├── src/
│   │   │   ├── components/
│   │   │   ├── utils/
│   │   │   ├── App.jsx
│   │   │   └── CyberpunkTheme.css
│   │   └── package.json
│   └── START_APP.bat # Автозапуск обоих серверов
├── config.py         # Конфигурация CLIProxy
├── run_factory.py    # Запуск мультиагентной системы
└── test_all_models.py # Тестирование моделей
```

## 🚀 Быстрый старт

### Требования
- Python 3.10+
- Node.js 18+
- CLIProxy (запущен на `http://127.0.0.1:8317`)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/Ntxfloy/MultiAgent_CLIProxy.git
cd MultiAgent_CLIProxy
```

2. **Установите зависимости Python**
```bash
pip install -r requirements.txt
```

3. **Установите зависимости Node.js**
```bash
cd workspace/frontend
npm install
cd ../..
```

4. **Настройте CLIProxy**
- Убедитесь что CLIProxy запущен на `http://127.0.0.1:8317`
- API ключ: `test-key-123` (настраивается в `config.py`)

### Запуск

#### Веб-интерфейс (рекомендуется)
```bash
cd workspace
START_APP.bat
```
Откройте http://localhost:5173

#### Мультиагентная система (CLI)
```bash
python run_factory.py "Создай простой калькулятор на Python"
```

## 🎨 Веб-интерфейс

### Возможности
- **Sidebar**: история разговоров, выбор модели, создание нового чата
- **Скрытие sidebar**: кнопка ◀/▶ в левом верхнем углу
- **Markdown**: автоматический рендеринг с подсветкой кода
- **Адаптивность**: работает на desktop, tablet, mobile
- **Локализация**: полностью на русском языке

### Горячие клавиши
- `Enter` - отправить сообщение
- `Shift+Enter` - новая строка в сообщении

## 🤖 Мультиагентная система

### Архитектура
```
User Prompt
    ↓
Orchestrator (gpt-5.2-codex)
    ↓
SelectorGroupChat
    ├── Coder 1 (gemini-2.5-flash)
    ├── Coder 2 (gemini-2.5-flash)
    ├── Coder 3 (gemini-2.5-flash)
    └── Reviewer (gpt-5.2-codex)
    ↓
Result
```

### Рабочие модели
- gpt-5.2-codex (Best)
- gpt-5.2
- gpt-5.1-codex-max
- gpt-5.1-codex
- gpt-5-codex
- gpt-5.1
- gpt-5
- gemini-2.5-pro
- gemini-2.5-flash
- gemini-2.5-flash-lite
- gemini-3-flash-preview
- gemini-2.5-computer-use-preview-10-2025
- tab_flash_lite_preview
- gpt-5-codex-mini
- gpt-5.1-codex-mini

### Fallback цепочки
Если модель недоступна, система автоматически переключается на резервную:
```
gpt-5.2-codex → gpt-5.1-codex → gpt-5-codex → gemini-2.5-pro
```

## 📝 Примеры использования

### CLI
```bash
# Простая задача
python run_factory.py "Напиши функцию для сортировки массива"

# Сложная задача
python run_factory.py "Создай REST API с аутентификацией и базой данных"

# Тестирование моделей
python test_all_models.py
```

### Веб-интерфейс
1. Запустите `START_APP.bat`
2. Откройте http://localhost:5173
3. Выберите модель из списка
4. Начните диалог

## 🛠️ Конфигурация

### config.py
```python
CLIPROXY_BASE_URL = "http://127.0.0.1:8317/v1"
CLIPROXY_API_KEY = "test-key-123"
DEFAULT_MODEL = "gpt-5.2-codex"
```

### Backend (workspace/backend/app/services/ai_service.py)
```python
CLIPROXY_URL = "http://127.0.0.1:8317/v1/chat/completions"
CLIPROXY_API_KEY = "test-key-123"
```

## 🐛 Troubleshooting

### CLIProxy не отвечает
```bash
# Проверьте что CLIProxy запущен
curl http://127.0.0.1:8317/v1/models
```

### Модели возвращают 403
- Проверьте API ключ в `config.py`
- Убедитесь что модель доступна в CLIProxy

### Frontend не запускается
```bash
cd workspace/frontend
npm install
npm run dev
```

### Backend не запускается
```bash
cd workspace/backend
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000
```

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

## 📧 Контакты

- GitHub: [@Ntxfloy](https://github.com/Ntxfloy)
- Репозиторий: [MultiAgent_CLIProxy](https://github.com/router-for-me/CLIProxyAPIPlus)


